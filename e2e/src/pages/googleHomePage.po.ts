import { element, by, ElementFinder } from 'protractor';

export class GoogleHomePage {
    
     aplications_button = element(by.id('gbwa'));
     google_translate_button = element(by.id('gb51'));

}
